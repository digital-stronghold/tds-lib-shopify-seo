name: Validate PR Title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: read

jobs:
  validate-pr-title:
    uses: digital-stronghold/tds-github-reusable-workflows/.github/workflows/validate-pr-title.yml@main
